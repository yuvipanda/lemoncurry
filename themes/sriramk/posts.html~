{% extends "base.html" %}
{% from "macros.html" import single_post %}

{% block title %}
{{ title }}
{% endblock %}

{% block content %}
<ul id="stories">
    {% for p in posts %}
   <li id="story">
     <a href="{{settings.base_url}}{{ p.meta.permalink }}" rel="bookmark">{{ p.title|escape }}</a> 
       <ul class='entry-tags'>
    {% for t in p.meta.tags %}
    <li>
    <a rel="tag" href="{{settings.base_url}}tag/{{ t }}.html">{{ t }}</a>
    </li>
    {% endfor %}
       </ul>
          


   </li>
    {% endfor %}
</ul>
{% endblock %}
